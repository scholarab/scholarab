---
import Layout from '../layouts/Layout.astro'
import { loadScholarships } from '../lib/data-loader'
import EligibilityQuiz from '../components/EligibilityQuiz'

const scholarships = await loadScholarships()
---

<Layout
  title="Find Your Scholarships — ScholarAB"
  description="Answer a few questions and instantly see every Alberta scholarship you qualify for. Your answers never leave your device."
  ogTitle="Find Your Scholarships — ScholarAB"
  ogDescription="Answer a few questions and instantly see every Alberta scholarship you qualify for. Your answers never leave your device."
  ogUrl="https://www.scholarab.ca/match"
  currentPath="/match"
>
  <main class="pb-16">
    <div class="max-w-xl mx-auto pt-8 md:pt-12">

      <!-- Header -->
      <div class="mb-8">
        <a href="/scholarships" class="text-xs text-gray-400 dark:text-white/30 hover:text-gray-700 dark:hover:text-white/60 transition inline-flex items-center gap-1 mb-4">
          ← All scholarships
        </a>
        <h1 class="font-bold text-gray-900 dark:text-white mb-2" style="font-size: clamp(22px, 3vw, 32px);">Find your scholarships</h1>
        <p class="text-sm text-gray-500 dark:text-white/45 mb-3">
          Answer 4 quick steps and we'll show you every scholarship you likely qualify for — with a total award amount.
        </p>
        <div class="flex items-center gap-2 text-xs text-gray-400 dark:text-white/25">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/>
          </svg>
          Your answers are matched locally on this device. Nothing is sent to a server or stored.
        </div>
      </div>

      <!-- Quiz -->
      <div class="bg-white dark:bg-white/[0.025] border border-gray-100 dark:border-white/[0.07] rounded-2xl p-6 md:p-8 shadow-sm dark:shadow-none">
        <EligibilityQuiz scholarships={scholarships} client:load />
      </div>

    </div>
  </main>
</Layout>
